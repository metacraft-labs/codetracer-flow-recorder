# Maintainer: Metacraft Labs <ci@metacraft-labs.com>
#
# Generated by codetracer-launcher/packaging/recorder-templates/generate.py
# from codetracer-flow-recorder/packaging/recorder-metadata.yml.
# DO NOT edit this file by hand — re-run the generator instead.
#
# PKGBUILD for codetracer-flow-recorder (CodeTracer recorder for Flow Cadence smart contracts).

pkgname=codetracer-flow-recorder
pkgver=0.1.0
pkgrel=1
pkgdesc="CodeTracer recorder for Flow Cadence smart contracts"
arch=('x86_64' 'aarch64')
url="https://github.com/metacraft-labs/codetracer-flow-recorder"
license=('Apache-2.0')
depends=('glibc' 'zstd')
makedepends=('rust' 'cargo')
provides=("codetracer-flow-recorder")
source=("$pkgname-$pkgver.tar.gz::https://github.com/metacraft-labs/codetracer-flow-recorder/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('REPLACE_AT_PUBLISH_TIME_source')

build() {
  cd "$srcdir/codetracer-flow-recorder-$pkgver"
  cargo build --release --locked --bin codetracer-flow-recorder
}

check() {
  cd "$srcdir/codetracer-flow-recorder-$pkgver"
  cargo test --release --locked --bin codetracer-flow-recorder || true
}

package() {
  cd "$srcdir/codetracer-flow-recorder-$pkgver"
  install -Dm755 "target/release/codetracer-flow-recorder" \
    "$pkgdir/usr/bin/codetracer-flow-recorder"
  if [[ -f LICENSE ]]; then
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
  fi
  if [[ -f README.md ]]; then
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
  fi
}
